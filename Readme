#Step 1. Import sandbox/build.gralde
#Step 2. IntelliJ -> right click to sandbox/build.gradle -> import project gradle
#Step 2. run 'gradle wrapper' - Please use Gradle 6.3 or later - https://github.com/gradle/gradle/issues/884

#Create distribution
./gradlew distZip

#Build docker image
docker build -t myrepo/sandbox-1.0 .

#run container
docker run --publish 8000:8080 -ti --detach --name sandbox myrepo/sandbox-1.0:latest

#Detach without exiting shell: CTRL + p or CTRL + q

